echo "passing only the file path not mount path as it picks from the workspace of this container where it is running"
echo "and the cloned project will be directly present in the workspace of the container"
bash src/main/java/org/example/jenkins/HelloWorld.sh